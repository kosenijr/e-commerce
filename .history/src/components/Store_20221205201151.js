import Layout from './Layout'

// create an array of objects within store.js


const Store = () => {
    return (
        <Layout >
            <Store />
        </Layout >

    )
}

export default Store