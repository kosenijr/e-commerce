import Layout from './Layout'

// create an array of objects within store


const Store = () => {
    return (
        <Layout >
            <Store />
        </Layout >

    )
}

export default Store